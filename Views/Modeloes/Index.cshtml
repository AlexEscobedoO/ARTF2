@model IEnumerable<ARTF2.Models.Modelo>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Modequi)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Modequi)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Idmod">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Idmod">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Idmod">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
